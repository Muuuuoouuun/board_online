import { territoryEngine } from "../games/territory.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real territory AI replaces this file. */
export const territoryAi = makeRandomAi(territoryEngine);
