import { gonuEngine } from "../games/gonu.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real gonu AI replaces this file. */
export const gonuAi = makeRandomAi(gonuEngine);
