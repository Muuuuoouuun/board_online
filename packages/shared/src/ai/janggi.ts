import { janggiEngine } from "../games/janggi.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real janggi AI replaces this file. */
export const janggiAi = makeRandomAi(janggiEngine);
