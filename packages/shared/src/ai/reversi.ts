import { reversiEngine } from "../games/reversi.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real reversi AI replaces this file. */
export const reversiAi = makeRandomAi(reversiEngine);
