import { flickEngine } from "../games/flick.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real flick AI replaces this file. */
export const flickAi = makeRandomAi(flickEngine);
