import { checkersEngine } from "../games/checkers.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real checkers AI replaces this file. */
export const checkersAi = makeRandomAi(checkersEngine);
