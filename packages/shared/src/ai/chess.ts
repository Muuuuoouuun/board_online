import { chessEngine } from "../games/chess.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real chess AI replaces this file. */
export const chessAi = makeRandomAi(chessEngine);
