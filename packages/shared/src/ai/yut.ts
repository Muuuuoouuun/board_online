import { yutEngine } from "../games/yut.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real yut AI replaces this file. */
export const yutAi = makeRandomAi(yutEngine);
