import { gomokuEngine } from "../games/gomoku.js";
import { makeRandomAi } from "./random.js";

/** Placeholder: plays random legal moves until a real gomoku AI replaces this file. */
export const gomokuAi = makeRandomAi(gomokuEngine);
