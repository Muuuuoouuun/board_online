# Only needed when the client is hosted separately from the Socket.IO server
# (e.g. static client on Vercel, server on Render). Leave unset when one
# container serves both — the client then talks to its own origin.
VITE_SERVER_URL=https://your-board-online-server.example.com
